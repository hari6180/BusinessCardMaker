import React from "react";

const Login = (props) => {
  return (
    <section>
      <h1>Login</h1>
      <button>Google</button>
      <button>Github</button>
    </section>
  );
};

export default Login;
